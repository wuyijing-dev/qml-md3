# md3qml

Install from the repo root:

```bash
pip install -e ./python[pyside6]
```

Or add `python/` to `PYTHONPATH` and `pip install PySide6`.

See [docs/topics/pyside.md](../docs/topics/pyside.md) and [examples/hello-pyside](../examples/hello-pyside/).
