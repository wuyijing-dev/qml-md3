# Md3HalfGauge

- **Source:** `src/Md3/components/Md3HalfGauge.qml`

Semicircle / half-dial gauge with a flat bottom.

## Properties

Same core as `Md3Gauge` (`value`, `from`, `to`, `label`, `unit`, `size`, `strokeWidth`, `valueColor`, `trackColor`) with default `startAngle: 180`, `sweepAngle: 180`.
